# Google Calendar API 配置
# Google API 凭证文件路径（从 Google Cloud Console 下载）
GOOGLE_CREDENTIALS_PATH=credentials.json

# Home Assistant 配置
# Home Assistant 实例的 URL（不要在末尾加斜杠）
HA_BASE_URL=http://192.168.1.100:8123

# Home Assistant 长期访问令牌
# 在 Home Assistant 中创建：个人资料 -> 长期访问令牌
HA_ACCESS_TOKEN=your_long_lived_access_token_here

# 小米小爱音箱配置
# 支持三种方式，按推荐顺序：
#
# 方式 1: Home Assistant Script（最推荐，最灵活）
# 格式：script.{脚本名称}
# 示例：script.xiaomi_speaker_say
# 优点：可以自定义消息格式、添加条件判断、组合多个动作
#
# 在 HA 中创建 Script 的步骤：
# 1. 打开 Home Assistant -> 设置 -> 自动化和场景 -> 脚本
# 2. 点击"添加脚本" -> "创建新脚本"
# 3. 添加以下配置：
#    - 别名：小爱播放文本
#    - 动作：
#      * 服务：notify.send_message 或 notify.{你的notify服务名}
#      * 消息：[\"{{ msg }}\"]
#      * 目标实体：notify.xiaomi_cn_83422613_s12_play_text_a_5_1
#    - 字段：
#      * 名称：msg
#      * 类型：文本
# 4. 保存后，脚本的 entity_id 就是 script.{你设置的名称}
#
# 方式 2: notify 服务（小米官方集成）
# 格式：notify.{设备名称}
# 示例：notify.xiaomi_cn_83422613_s12_play_text_a_5_1
# 在 HA 开发者工具 -> 服务 中搜索 "notify" 查找
#
# 方式 3: media_player 实体（传统方式，不推荐）
# 格式：media_player.{设备名称}
# 示例：media_player.xiaoai_speaker_xxxxx
#
# 根据你的实际情况选择一种配置方式：
XIAOMI_SPEAKER_ENTITY_ID=script.xiaomi_speaker_say

# 提醒设置
# 消息模板：支持以下占位符
# {event_name} - 日程名称（会自动移除 [数字] 标记）
# {minutes} - 距离开始的分钟数
#
# 示例模板：
# - 提醒：{event_name} 将在 {minutes} 分钟后开始
# - {event_name} 还有 {minutes} 分钟
# - 注意：{minutes} 分钟后需要参加 {event_name}
REMINDER_MESSAGE_TEMPLATE=提醒：{event_name} 将在 {minutes} 分钟后开始

# 提醒策略（自动根据日程标题判断）：
# - 普通日程：5分钟前、1分钟前（两次提醒）
# - 标记日程：在标题中使用 [数字] 表示需要额外提前的时间
#   例如："去机场 [30]" 会在 35分钟前(5+30)、31分钟前(1+30) 提醒
#   标记范围：1-60 分钟

# 检查日历的间隔时间（秒，默认 60 秒）
CHECK_INTERVAL=60

# 健康检查通知时间段（避免打扰休息）
# 只在指定的时间段内发送健康检查警报
# 超出这个时间段，即使检测到故障也不会发送语音通知（但会记录日志）
# 时间格式：24小时制（0-23）
# 默认：17:00 - 21:00（下午5点到晚上9点）
HEALTH_ALERT_START_HOUR=17
HEALTH_ALERT_END_HOUR=21

